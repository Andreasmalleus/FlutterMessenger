import 'package:flutter/material.dart';

class SearchedMessages extends StatelessWidget{

  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text("SearchedMessages"),
      ),
      body: Container(
        child: Text("SearchedMessages"),
      ),
    );
  }
}